2017-8-25
	今天在测试mount命令额时候发现一个奇怪的问题,我在windows下测试u盘是可读写的,但是在linux上我利用fsck -C -a /dev/sdb1命令修复之后只有在/media/下自动挂载之后才可以制度写,我一旦手动挂载就变成了只读(虽然mount显示他是可读写的,不知道为什么)

问题结局:
    权限的问题
　　自动挂载的时候是用当前用户挂载的，但是我手动挂载需要动用root，之后出现的无法写的问题是因为普通用户对手动挂载文件没有写权限，只有超级用户存在对手动挂载文件的写权限
