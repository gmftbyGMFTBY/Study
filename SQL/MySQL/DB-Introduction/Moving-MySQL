MySQL

1.数据库组成：
    1.系统数据库：
    系统数据库是我们安装之后会附带的一写数据库，会记录一些数据库管理系统的必要信息，用户不能直接对这些信息进行修改
        1.2 information_schema:
            存储了一些数据库对象的信息，比如用户表信息，列信息，权限信息，字符集信息，分区信息等等
        1.3 performance_schema:
            数据库服务器端的性能参数
        1.4 mysql
            主要存储了系统的用户权限信息
        1.5 test:
            自动创建的测试数据库
    2.用户数据库：
    用户数据库是用户自定义的数据库
    爱数据库中存在这很多的数据库对象，数据库对象指的是我们的数据的存储，管理，使用数据的不同的形式，主要包含我们的表，视图，存储过程，函数，触发器和事件


2.数据库的存储引擎和数据类型：

