2017.2.22
    \x79 的含义是一个字节，这个字节中的前 4 为是 7 的二进制就是 0111, 后 4 为是 9 的二进制是 1001, 拼接起来的二进制就是 01111001 -> 121(十进制)，查看 ASCII 表后知道是字母 'y'
    ```python
    # result is 'y' 
    b'\x79'.decode('utf8')
    ```
