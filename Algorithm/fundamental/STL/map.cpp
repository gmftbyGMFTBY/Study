#include <stdio.h>
#include <iostream>
#include <stdlib.h>
#include <map>

/*
   map<string, int>M;   string -> to map
   M.clear();

   if (M.find(a) == M.end())    // a is string

   M[a] = b; a is string, b is int
 */

using namespace std;

int main () {

    return 0;
}
