1. uptime
    1. 告知系统的实际运行的时间和当前系统的相关信息
    2. 显示格式
        当前时间 系统的实际运行时间 当前的登录用户数目 系统的平局负载(1, 5, 15分钟的平均负载)
    3. 系统平均负载
        在可运行状态或者是非中断状态下的系统的平局的进程数量
        * 可运行 : 
            1. 进程就绪
            2. 进程运行
        * 非中断
            1. 等待I/O
            2. 等待磁盘调度
    4. 显示参数
        * -p : 良好显示结果
        * -h : 显示帮助信息
        * -s : 显示系统的启动时间
        * -v : 显示版本号
